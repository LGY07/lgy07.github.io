<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>在NVMe硬盘安装ArchLinux+Windows11</title>
    <url>/2021/12/19/001/</url>
    <content><![CDATA[<h1 id="如何安装ArchLinux和Windows11在NVMe硬盘："><a href="#如何安装ArchLinux和Windows11在NVMe硬盘：" class="headerlink" title="如何安装ArchLinux和Windows11在NVMe硬盘："></a>如何安装ArchLinux和Windows11在NVMe硬盘：</h1><hr>
<p>（第一次写博客，可能很烂（</p>
<h5 id="本文主要解决以下问题："><a href="#本文主要解决以下问题：" class="headerlink" title="本文主要解决以下问题："></a>本文主要解决以下问题：</h5><p><code>1.ArchLinux无法识别较新的NVMe（我的是金士顿A2000）</code></p>
<p><code>2.os-prober无法识别Windows11</code></p>
<h5 id="需要准备："><a href="#需要准备：" class="headerlink" title="需要准备："></a>需要准备：</h5><p><code>一台正常装有Windows11的电脑</code></p>
<p><code>一个耐用的U盘（最好是移动硬盘，也可以是热插拔的SATA盘），以下简称“U盘1”</code></p>
<p><code>一个烧录了ArchLive的U盘，以下简称“U盘2”</code></p>
<p><code>一个烧录了wepe的U盘，以下简称“U盘3”</code><a href="https://www.wepe.com.cn/download.html">WePE-下载</a></p>
<p><del><code>一个没有坏掉的大脑（（（</code></del></p>
<h2 id="——ArchLinux在NVMe如何安装："><a href="#——ArchLinux在NVMe如何安装：" class="headerlink" title="——ArchLinux在NVMe如何安装："></a>——ArchLinux在NVMe如何安装：</h2><p>进入U盘2的Arch Live，并插上U盘1</p>
<p>在U盘1上安装Arch，更改以下步骤：</p>
<figure class="highlight open plaintext"><span class="code-header">    <span class="code-title">      <div class="code-icon"></div>TEXT 共 1 行</span>      <span class="code-header-tail">        <button class="code-copy"></button>        <span class="code-space">展开</span></span></span></span>    <div class="code-box"><table><tr><td class="code"><pre><code class="hljs plaintext">pacstrap /mnt base linux-zen linux-firmware<br></code></pre></td></tr></table></div></figure>
<p>进入chroot环境后，安装arch安装脚本：</p>
<figure class="highlight open plaintext"><span class="code-header">    <span class="code-title">      <div class="code-icon"></div>TEXT 共 1 行</span>      <span class="code-header-tail">        <button class="code-copy"></button>        <span class="code-space">展开</span></span></span></span>    <div class="code-box"><table><tr><td class="code"><pre><code class="hljs plaintext">pacman -S arch-install-scritps<br></code></pre></td></tr></table></div></figure>
<p>不必安装图形界面，重启后fdisk -l，就能看到NVMe硬盘了</p>
<p>按照在SATA盘安装Arch的步骤，在NVMe盘安装ArchLinux，引导分区使用Windows11的ESP分区（建议先把ESP分区删了，重新创建，因为它太小了）</p>
<p>重启后，就可以进入ArchLinux了（好耶！</p>
<h2 id="——Grub2引导ArchLinux和Windows11："><a href="#——Grub2引导ArchLinux和Windows11：" class="headerlink" title="——Grub2引导ArchLinux和Windows11："></a>——Grub2引导ArchLinux和Windows11：</h2><p>（上面的ArchLinux请使用Grub2引导</p>
<p>进入U盘3的wepe</p>
<p>打开dism++（启动时的东西都不用管）</p>
<p>选择Windows11</p>
<p>点击“工具箱”，点击引导修复，选择ArchLinux的引导分区</p>
<p>修复完成后重启系统，会正常进入Windows11，但是并没有破坏掉Grub2，启动按F12（我的是F12，请自己看主板说明，或者F*全部按一遍）进入引导菜单，找到ArchLinux（具体的名称是你安装Grub2时设置的）</p>
<figure class="highlight open plaintext"><span class="code-header">    <span class="code-title">      <div class="code-icon"></div>TEXT 共 6 行</span>      <span class="code-header-tail">        <button class="code-copy"></button>        <span class="code-space">展开</span></span></span></span>    <div class="code-box"><table><tr><td class="code"><pre><code class="hljs plaintext">#grub-install --target=x86_64 --efi-directory=/boot --bootloader-<br>id=ArchLinux<br><br>#pacman -S vim<br><br>#vim /etc/grub.d/40_custom<br></code></pre></td></tr></table></div></figure>
<p>编辑40_custom脚本：</p>
<figure class="highlight fold plaintext"><span class="code-header">    <span class="code-title">      <div class="code-icon"></div>TEXT 共 21 行</span>      <span class="code-header-tail">        <button class="code-copy"></button>        <span class="code-space">展开</span></span></span></span>    <div class="code-box"><table><tr><td class="code"><pre><code class="hljs plaintext">### BEGIN /etc/grub.d/40_custom_proxy ###<br><br>menuentry &quot;Windows11&quot;  &#123;<br><br>insmod part_gpt<br><br>insmod fat<br><br>insmod chain<br><br>root=uuid= 【引导分区的文件系统UUID】<br><br>search --no-floppy --fs-uuid --set=root 【引导分区的文件系统UUID】 --hint-<br>bios=hd0,gpt0 --hint-efi=hd0,gpt0 --hint-baremetal=NVMe0,gpt0<br>【Windows11的系统分区文件系统UUID】<br><br>chainloader /EFI/Microsoft/Boot/bootmgfw.efi<br><br>&#125;<br><br>### END /etc/grub.d/40_custom_proxy ###<br></code></pre></td></tr></table></div></figure>
<p>确保你的Windows11位于硬盘第一个分区（&#x2F;dev&#x2F;NVMe0n1p1）</p>
<p>如果不是，请将上面的两个hd0改为hd*，&#x2F;dev&#x2F;NVMe0n1pX</p>
<p>*应为X-1</p>
<figure class="highlight open plaintext"><span class="code-header">    <span class="code-title">      <div class="code-icon"></div>TEXT 共 1 行</span>      <span class="code-header-tail">        <button class="code-copy"></button>        <span class="code-space">展开</span></span></span></span>    <div class="code-box"><table><tr><td class="code"><pre><code class="hljs plaintext">#grub-makecfg -o /boot/grub/grub.cfg<br></code></pre></td></tr></table></div></figure>
<p>（如果你之前装的grub配置文件为”grub.conf”，请将以上命令的”grub.cfg”改为”grub.conf”）</p>
<h5 id="引导部分可以自己看看ArchWiki："><a href="#引导部分可以自己看看ArchWiki：" class="headerlink" title="引导部分可以自己看看ArchWiki："></a>引导部分可以自己看看ArchWiki：</h5><p><a href="https://wiki.archlinux.org/title/GRUB_(%E7%AE%80%E4%BD%93%E4%B8%AD%E6%96%87)#%E5%A4%9A%E7%B3%BB%E7%BB%9F%E5%90%AF%E5%8A%A8">GRUB (简体中文) - ArchWiki (archlinux.org)</a></p>
]]></content>
  </entry>
  <entry>
    <title>在任意Linux发行版安装pacman软件包管理器</title>
    <url>/2022/01/06/002/</url>
    <content><![CDATA[<h1 id="在任意的Linux发行版使用pacman软件包管理器"><a href="#在任意的Linux发行版使用pacman软件包管理器" class="headerlink" title="在任意的Linux发行版使用pacman软件包管理器"></a>在任意的Linux发行版使用pacman软件包管理器</h1><hr>
<h5 id="作为Arch母亲的忠实用户，你是否为其他的Linux发行版没有pacman感到烦恼"><a href="#作为Arch母亲的忠实用户，你是否为其他的Linux发行版没有pacman感到烦恼" class="headerlink" title="作为Arch母亲的忠实用户，你是否为其他的Linux发行版没有pacman感到烦恼"></a>作为Arch<del>母亲</del>的忠实用户，你是否为其他的Linux发行版没有pacman感到烦恼</h5><h4 id="用优秀的灵格丁先生写的pacman-installer脚本！"><a href="#用优秀的灵格丁先生写的pacman-installer脚本！" class="headerlink" title="用优秀的灵格丁先生写的pacman-installer脚本！"></a>用优秀的<a href="https://github.com/lingrottin">灵格丁</a>先生写的<a href="https://github.com/LGY07/pacman-installer">pacman-installer</a>脚本！</h4><p>dnf用户可以使用<code>$sudo dnf install pacman</code>来安装，但是需要更改<code>/etc/pacman.conf</code>的配置文件，改变根目录,需要更改的部分：</p>
<figure class="highlight open plaintext"><span class="code-header">    <span class="code-title">      <div class="code-icon"></div>TEXT 共 4 行</span>      <span class="code-header-tail">        <button class="code-copy"></button>        <span class="code-space">展开</span></span></span></span>    <div class="code-box"><table><tr><td class="code"><pre><code class="hljs plaintext">RootDir     = /你希望用来安装软件的目录/<br>DBPath      = /你希望用来安装软件的目录/var/lib/pacman/<br>CacheDir    = /你希望用来安装软件的目录/var/cache/pacman/pkg/<br>LogFile     = /你希望用来安装软件的目录/var/log/pacman.log<br></code></pre></td></tr></table></div></figure>
<p>然后创建目录，确保以上目录存在：</p>
<figure class="highlight open plaintext"><span class="code-header">    <span class="code-title">      <div class="code-icon"></div>TEXT 共 8 行</span>      <span class="code-header-tail">        <button class="code-copy"></button>        <span class="code-space">展开</span></span></span></span>    <div class="code-box"><table><tr><td class="code"><pre><code class="hljs plaintext">$sudo mkdir /你希望用来安装软件的目录/<br>$sudo mkdir /你希望用来安装软件的目录/var/<br>$sudo mkdir /你希望用来安装软件的目录/var/lib/<br>$sudo mkdir /你希望用来安装软件的目录/var/lib/pacman/<br>$sudo mkdir /你希望用来安装软件的目录/var/cache/<br>$sudo mkdir /你希望用来安装软件的目录/var/cache/pacman/<br>$sudo mkdir /你希望用来安装软件的目录/var/cache/pacman/<br>$sudo mkdir /你希望用来安装软件的目录/var/log/<br></code></pre></td></tr></table></div></figure>

<br/>

<p>如果是其他用户，请使用<a href="https://github.com/LGY07/pacman-installer">pacman-installer</a>安装脚本,你可以按照READEME的标准安装方式，也可以使用以下命令快捷安装：</p>
<p>wget下载器：</p>
<figure class="highlight open plaintext"><span class="code-header">    <span class="code-title">      <div class="code-icon"></div>TEXT 共 1 行</span>      <span class="code-header-tail">        <button class="code-copy"></button>        <span class="code-space">展开</span></span></span></span>    <div class="code-box"><table><tr><td class="code"><pre><code class="hljs plaintext">$su - -c &#x27;bash &lt;(wget -O- https://github.com/LGY07/pacman-installer/raw/main/pacman-installer.sh)&#x27;<br></code></pre></td></tr></table></div></figure>
<p>curl下载器：</p>
<figure class="highlight open plaintext"><span class="code-header">    <span class="code-title">      <div class="code-icon"></div>TEXT 共 1 行</span>      <span class="code-header-tail">        <button class="code-copy"></button>        <span class="code-space">展开</span></span></span></span>    <div class="code-box"><table><tr><td class="code"><pre><code class="hljs plaintext">$su - -c &#x27;bash &lt;(curl https://github.com/LGY07/pacman-installer/raw/main/pacman-installer.sh)&#x27;<br></code></pre></td></tr></table></div></figure>]]></content>
  </entry>
  <entry>
    <title>使用脚本安装ArchLinux(不一定成功)</title>
    <url>/2022/01/08/003/</url>
    <content><![CDATA[<h1 id="ArchLinux-安装教程（不完全是）"><a href="#ArchLinux-安装教程（不完全是）" class="headerlink" title="ArchLinux 安装教程（不完全是）"></a>ArchLinux 安装教程（不完全是）</h1><hr>
<p><img data-src="/images/Linux.png" alt="Linux的选择.png"></p>
<p>学习 ArchLinux<code>找不到教程，看不懂官方文档，嫌麻烦</code>是很大的问题</p>
<p>为了帮助便捷地安装 ArchLinux，我写了一个<a href="https://github.com/LGY07/arch-install">交互式安装脚本</a>(<del>祸害</del>)</p>
<p>优势在于可以通过以下几步快速的完成 ArchLinux 的安装（已经有别的安装脚本，但是个人认为比正常安装还麻烦</p>
<h3 id="1-下载镜像"><a href="#1-下载镜像" class="headerlink" title="1.下载镜像"></a>1.下载镜像</h3><p><a href="https://mirrors.tuna.tsinghua.edu.cn/archlinux/iso/latest/">清华大学镜像站</a></p>
<h3 id="2-烧录镜像"><a href="#2-烧录镜像" class="headerlink" title="2.烧录镜像"></a>2.烧录镜像</h3><p>可以使用<a href="https://rufus.ie/zh/">Rufus</a>烧录（一定要 FAT32 而不是 NTFS）</p>
<p>或者直接打开镜像，把里面的文件全部复制到一个格式为 FAT32 的 U 盘里</p>
<h3 id="3-重启电脑进入镜像"><a href="#3-重启电脑进入镜像" class="headerlink" title="3.重启电脑进入镜像"></a>3.重启电脑进入镜像</h3><p>（混完 3 个步骤的字数了</p>
<h2 id="4-下载安装脚本"><a href="#4-下载安装脚本" class="headerlink" title="4.下载安装脚本"></a>4.下载安装脚本</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">curl -O https://huashijituan.coding.net/p/liuzhierzhong/d/liuzhierzhong/git/raw/master/install.sh</span><br></pre></td></tr></table></figure>

<p><del>这可能是最困难的步骤，需要视力以及打字速度，但是好在它不需要更改</del></p>
<h2 id="5-运行脚本"><a href="#5-运行脚本" class="headerlink" title="5.运行脚本"></a>5.运行脚本</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">bash ./install.sh</span><br></pre></td></tr></table></figure>

<p>在脚本中，需要进行的步骤：</p>
<h3 id="分区：使用-cfdisk-分区，选择-gpt"><a href="#分区：使用-cfdisk-分区，选择-gpt" class="headerlink" title="分区：使用 cfdisk 分区，选择 gpt"></a>分区：使用 cfdisk 分区，选择 gpt</h3><p>使用 ⬆⬇ 键选择位置，选择空闲的空间</p>
<p>使用 ⬅➡ 键选择对当前位置的操作，选择 New 创建分区</p>
<p>输入引导分区大小为 1G（可以很小，<del>100M 也行</del>，但是不推荐）</p>
<p>输入根目录分区大小最好在 10G 以上（<del>也许 2G 也行，足以体现 ArchLinux 的轻量</del>）</p>
<p>选择 Write，输入 yes，选择 Quit 退出</p>
<h3 id="选择分区"><a href="#选择分区" class="headerlink" title="选择分区"></a>选择分区</h3><p>输入刚才的根目录分区位置(例如 sdaX，Nvme 盘为 nvme0n1pX)</p>
<p>输入引导分区位置</p>
<h3 id="选择内核"><a href="#选择内核" class="headerlink" title="选择内核"></a>选择内核</h3><p>默认 linux-zen</p>
<h3 id="选择桌面环境"><a href="#选择桌面环境" class="headerlink" title="选择桌面环境"></a>选择桌面环境</h3><p>默认 KDE Plasma</p>
<h3 id="选择文本编辑器"><a href="#选择文本编辑器" class="headerlink" title="选择文本编辑器"></a>选择文本编辑器</h3><p>默认 Vim</p>
<h3 id="选择-AUR-助手"><a href="#选择-AUR-助手" class="headerlink" title="选择 AUR 助手"></a>选择 AUR 助手</h3><p>默认 Yay</p>
<h3 id="接下来会自动-pacstrap-安装系统"><a href="#接下来会自动-pacstrap-安装系统" class="headerlink" title="接下来会自动 pacstrap 安装系统"></a>接下来会自动 pacstrap 安装系统</h3><p>完成后提示输入用户名，设置你的用户名，不要有空格</p>
<p>设置用户的密码，输入两次，不会显示出来，甚至输入后没变化，是正常现象（下同）</p>
<p>设置 root 的密码</p>
<h2 id="完成安装"><a href="#完成安装" class="headerlink" title="完成安装"></a>完成安装</h2><p>显示 FINISH，10 秒后自动重启</p>
<br/>
]]></content>
  </entry>
</search>
